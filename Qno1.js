// Qno1 Given an array of integers, use the `map` method to square each element and return a new array with the squared values.

const arr = [1,2,3,4,5];
const squareArr = arr.map(arr =>arr * arr);
console.log(squareArr);
